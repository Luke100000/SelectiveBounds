name: Java CI with Gradle

on:
  push:
    branches-ignore:
      - 'l10n*'
    tags:
      - '*'
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      MODRINTH_ID: ${{ secrets.MR_PROJECT_ID }}
      MODRINTH_TOKEN: ${{ secrets.MR_API_TOKEN }}
      CURSEFORGE_ID: ${{ secrets.CF_PROJECT_ID }}
      CURSEFORGE_TOKEN: ${{ secrets.CF_API_TOKEN }}
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Checkout Commit
        uses: actions/checkout@v2
      - name: Fetch Tags
        run: git fetch --unshallow --tags
      - name: Set up JDK 21
        uses: actions/setup-java@v1
        with:
          java-version: 21
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
      - name: Build with Gradle
        run: ./gradlew build
      - name: Get tag
        if: ${{ success() && startsWith(github.ref, 'refs/tags/') }}
        id: tag
        uses: dawidd6/action-get-tag@v1
      - uses: jungwinter/split@v2
        id: split
        with:
          separator: "+"
          msg: ${{steps.tag.outputs.tag}}
      - name: "Upload Fabric"
        uses: Kir-Antipov/mc-publish@v3.3.1
        if: ${{ success() && startsWith(github.ref, 'refs/tags/') }}
        with:
          modrinth-id: ${{ env.MODRINTH_ID }}
          modrinth-token: ${{ env.MODRINTH_TOKEN }}
          curseforge-id: ${{ env.CURSEFORGE_ID }}
          curseforge-token: ${{ env.CURSEFORGE_TOKEN }}

          name: "[Fabric ${{steps.split.outputs._1}}] SelectiveBounds - ${{steps.split.outputs._0}}"
          changelog-file: changelog.md
          version-type: release
          files: "fabric/build/libs/selectivebounds-fabric-${{steps.split.outputs._1}}-${{steps.split.outputs._0}}.jar"
          loaders: |
            fabric
          environment: client
          game-versions: |
            ${{steps.split.outputs._1}}
            1.21.1
          java: |
            Java 21
          dependencies: |
            fabric-api | required | *

          fail-mode: skip
      - name: "Upload Forge"
        uses: Kir-Antipov/mc-publish@v3.3.1
        if: ${{ success() && startsWith(github.ref, 'refs/tags/') }}
        with:
          modrinth-id: ${{ env.MODRINTH_ID }}
          modrinth-token: ${{ env.MODRINTH_TOKEN }}
          curseforge-id: ${{ env.CURSEFORGE_ID }}
          curseforge-token: ${{ env.CURSEFORGE_TOKEN }}

          name: "[Forge ${{steps.split.outputs._1}}] SelectiveBounds - ${{steps.split.outputs._0}}"
          changelog-file: changelog.md
          version-type: release
          files: "forge/build/libs/selectivebounds-forge-${{steps.split.outputs._1}}-${{steps.split.outputs._0}}.jar"
          loaders: |
            forge
          environment: client
          game-versions: |
            ${{steps.split.outputs._1}}
            1.21.1
          java: |
            Java 21

          fail-mode: skip
      - name: "Upload NeoForge"
        uses: Kir-Antipov/mc-publish@v3.3.1
        if: ${{ success() && startsWith(github.ref, 'refs/tags/') }}
        with:
          modrinth-id: ${{ env.MODRINTH_ID }}
          modrinth-token: ${{ env.MODRINTH_TOKEN }}
          curseforge-id: ${{ env.CURSEFORGE_ID }}
          curseforge-token: ${{ env.CURSEFORGE_TOKEN }}

          name: "[NeoForge ${{steps.split.outputs._1}}] SelectiveBounds - ${{steps.split.outputs._0}}"
          changelog-file: changelog.md
          version-type: release
          files: "neoforge/build/libs/selectivebounds-neoforge-${{steps.split.outputs._1}}-${{steps.split.outputs._0}}.jar"
          loaders: |
            neoforge
          environment: client
          game-versions: |
            ${{steps.split.outputs._1}}
            1.21.1
          java: |
            Java 21

          fail-mode: skip